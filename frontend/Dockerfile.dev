# Dev image — hot-reloading Next.js server. Production uses a multi-stage standalone build.
FROM node:26-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev"]
